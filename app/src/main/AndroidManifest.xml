<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bash.cashbook">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CashBook">
        <activity android:name="com.bash.cashbook.SettingActivity"/>
        <activity android:name="com.bash.cashbook.PengeluaranActivity" />
        <activity android:name="com.bash.cashbook.PemasukanActivity" />
        <activity android:name="com.bash.cashbook.DetailCashFlowActivity" />
        <activity android:name="com.bash.cashbook.MainActivity" />
        <activity android:name="com.bash.cashbook.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>